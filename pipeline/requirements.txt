requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
# Optional: only needed if ufcstats serves a JavaScript challenge from your network.
# playwright>=1.44   ->  pip install playwright && playwright install chromium
